<h3><%= @oven.name %></h3>

<div class="cookie-info">
  Cookie in oven:
  <% if @oven.cookie %>
    1 cookie with <%= @oven.cookie.fillings || "no fillings" %>
    <% if @oven.cookie.ready? %>
      (Your Cookie is Ready)
      <%= button_to "Retrieve Cookie", empty_oven_path, class: 'button tiny' %>
    <% end %>
  <% else %>
    None
  <% end %>
</div>

<br/>
<%= link_to "Prepare Single Cookie", new_oven_cookies_path(@oven, batch: false), class: 'button' %><br>
<%= link_to "Prepare Batch Of Cookie", new_oven_cookies_path(@oven, batch: true), class: 'button' %>